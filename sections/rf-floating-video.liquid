{%- style -%}
  .rf-floating-container {
    position: fixed;
    bottom: 20px;
    left: 20px;
    width: {{ section.settings.width_desktop }}px;
    aspect-ratio: 9 / 16;
    z-index: 2147483647;
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 10px 25px rgba(0,0,0,0.2);
    transition: all 0.3s ease;
    opacity: 0;
    visibility: hidden;
    transform: translateY(20px);
    background: #000;
  }
  .rf-floating-container.rf-visible {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
  }
  .rf-floating-container.rf-closed {
    display: none !important;
  }
  .rf-video-wrapper {
    width: 100%;
    height: 100%;
    position: relative;
    background: #000;
  }
  .rf-video-element,
  .rf-video-wrapper iframe {
    width: 100%;
    height: 100%;
    object-fit: cover;
    border: none;
    display: block;
  }
  .rf-close-btn {
    position: absolute;
    top: 8px;
    right: 8px;
    background: rgba(0, 0, 0, 0.5);
    color: #fff;
    border: none;
    border-radius: 50%;
    width: 24px;
    height: 24px;
    cursor: pointer;
    z-index: 20;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 14px;
    line-height: 1;
    backdrop-filter: blur(4px);
  }
  .rf-close-btn:hover {
    background: rgba(0, 0, 0, 0.8);
  }
  @media screen and (max-width: 768px) {
    .rf-floating-container {
      width: 140px;
      bottom: 80px;
      left: 15px;
    }
  }
{%- endstyle -%}

<div id="rf-floating-video-{{ section.id }}" class="rf-floating-container">
  <button class="rf-close-btn" aria-label="Close video" onclick="closeRfVideo(this)">
    &times;
  </button>

  <div class="rf-video-wrapper">
    {%- if section.settings.video_upload != blank -%}
      {{ section.settings.video_upload | video_tag: 
        image_size: '300x', 
        autoplay: true, 
        loop: true, 
        muted: true, 
        controls: false, 
        playsinline: true,
        class: 'rf-video-element' 
      }}
    {%- elsif section.settings.tiktok_url != blank -%}
      {%- assign tk_id = section.settings.tiktok_url -%}
      {%- if tk_id contains '/video/' -%}
        {%- assign tk_id = tk_id | split: '/video/' | last | split: '?' | first | split: '/' | first -%}
      {%- endif -%}
      
      <iframe 
        src="https://www.tiktok.com/embed/v2/{{ tk_id }}?lang=en-US&autoplay=1"
        allow="encrypted-media;"
        style="width: 100%; height: 100%; border: none;">
      </iframe>
    {%- else -%}
      <div style="display:flex;align-items:center;justify-content:center;height:100%;color:#fff;font-size:12px;text-align:center;padding:10px;">
        No Video Source
      </div>
    {%- endif -%}
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    var container = document.getElementById('rf-floating-video-{{ section.id }}');
    if (!sessionStorage.getItem('rf_video_closed')) {
      setTimeout(function() {
        container.classList.add('rf-visible');
      }, 1000);
    }
  });

  function closeRfVideo(btn) {
    var container = btn.closest('.rf-floating-container');
    container.classList.remove('rf-visible');
    setTimeout(function() {
      container.classList.add('rf-closed');
    }, 300);
    sessionStorage.setItem('rf_video_closed', 'true');
  }
</script>

{% schema %}
{
  "name": "RF Floating Video",
  "tag": "section",
  "class": "section-rf-floating-video",
  "settings": [
    {
      "type": "header",
      "content": "Video Source"
    },
    {
      "type": "video",
      "id": "video_upload",
      "label": "Upload Video (MP4 - Recommended)",
      "info": "Best for performance and autoplay."
    },
    {
      "type": "text",
      "id": "tiktok_url",
      "label": "TikTok Video Link",
      "info": "Paste the full TikTok URL (e.g., https://www.tiktok.com/@user/video/123...)"
    },
    {
      "type": "header",
      "content": "Appearance"
    },
    {
      "type": "range",
      "id": "width_desktop",
      "min": 100,
      "max": 400,
      "step": 10,
      "unit": "px",
      "label": "Desktop Width",
      "default": 180
    }
  ],
  "presets": [
    {
      "name": "RF Floating Video"
    }
  ]
}
{% endschema %}